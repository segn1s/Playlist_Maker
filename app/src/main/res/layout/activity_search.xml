<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root_search"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:paddingStart="@dimen/padding_search"
    android:paddingEnd="@dimen/padding_search"
    android:background="@color/background">

    <!-- Назад и Заголовок -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingBottom="@dimen/padding_search">

        <ImageView
            android:id="@+id/backButton"
            android:layout_width="@dimen/default_icon_width"
            android:layout_height="@dimen/default_icon_width"
            android:src="@drawable/arrow_left"
            app:tint="@color/ic_arrow_back_color"
            android:layout_marginEnd="0dp"/>

        <TextView
            android:id="@+id/tv_title"
            android:fontFamily="@font/ys_display_bold"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/padding_search"
            android:text="@string/search"
            android:textSize="22sp"
            android:textColor="@color/ic_arrow_back_color"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Поле поиска -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="@drawable/bg_search"
        android:paddingHorizontal="12dp"
        android:paddingVertical="8dp"
        android:gravity="center_vertical">

        <!-- Иконка поиска -->
        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="20dp"
            android:src="@drawable/search_icon"
            app:tint="@color/ic_search_color" />

        <FrameLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp">

            <EditText
                android:id="@+id/searchEditText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@android:color/transparent"
                android:hint="@string/search_hint"
                android:textColor="@color/search_field_text"
                android:textColorHint="@color/ic_search_color"
                android:inputType="text"
                android:maxLines="1"
                android:ellipsize="end"/>

            <ImageView
                android:id="@+id/clearButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@drawable/ic_clear_arrow_16"
                app:tint="@color/clear_button"
                android:layout_gravity="end|center_vertical"
                android:visibility="gone"/>
        </FrameLayout>

    </LinearLayout>

</LinearLayout>